# Shell Script Style

All shell script code must follow the [Google Shell Style Guide](https://google.github.io/styleguide/shellguide.html).

## Exception

All shell variable names must be `UPPER_SNAKE_CASE`, regardless of scope. This overrides any guidance in the style guide that would permit lowercase names for shell variables.
