version: "2"  # requires golangci-lint >= v1.62.0

linters:
  enable:
    - errcheck
    - govet
    - staticcheck

formatters:
  enable:
    - gofmt
    - goimports

issues:
  exclude-dirs:
    - internal/gen
